generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model posts {
  user_id Int
  post_id Int     @id
  title   String?
  content String?
  contact String?
}
